package Java_23_ArrayList;

public class Alist_06_MethodsAList {
	
	//add( Object o): This method adds an object o to the arraylist.
	//obj.add("hello");
	
	//add(int index, Object o): It adds the object o to the array list at the given index.
	//obj.add(2, "bye");
	
	//remove(Object o): Removes the object o from the ArrayList.
	//obj.remove("Chaitanya");
	
	//remove(int index): Removes element from a given index.
	//obj.remove(3);
	
	//set(int index, Object o): Used for updating an element. It replaces the element 
	//present at the specified index with the object o.
	//obj.set(2, "Tom");
	
	//int indexOf(Object o): Gives the index of the object o. If the element is not found in the 
	//list then this method returns the value -1.
	//int pos = obj.indexOf("Tom");
	
	//Object get(int index): It returns the object of list which is present at the specified index.
	//String str= obj.get(2);
	
	//int size(): It gives the size of the ArrayList – Number of elements of the list.
	//int numberofitems = obj.size();
	
	//boolean contains(Object o): It checks whether the given object o is present in the array list 
	//if its there then it returns true else it returns false.
	//obj.contains("Steve");
	
	//clear(): It is used for removing all the elements of the array list in one go. The below 
	//code will remove all the elements of ArrayList whose object is obj.
	//obj.clear();

	
	

}
